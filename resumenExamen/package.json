{
  "name": "resumenexamen",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "json-server": "json-server --watch src/data/db.json --port 3000",
    "examen": "concurrently \"npm run json-server\" \"npm run dev"
  },
  "devDependencies": {
    "vite": "^6.0.1"
  },
  "dependencies": {
    "concurrently": "^9.1.0",
    "json-server": "^1.0.0-beta.3"
  }
}
